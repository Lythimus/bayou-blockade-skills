# Verified, report-relevant findings (🔴)

> Populated per section as verification completes. Each entry: **Q#**, finding, source citation, flags.
> Only findings flagged `🔴` (REPORT-RELEVANT) with `✅ VERIFIED` or `⚠️ CORRECTED` accuracy belong here — `❌ UNVERIFIED` or `⚪ PARKED` findings stay in the section file, not here.

## Section {{NN}} — {{Section Title}}

- **Q{{n}}** — {{finding, one to two sentences}} — {{file}}:{{line}} — `{{accuracy flag}} {{relevance flag}}`
